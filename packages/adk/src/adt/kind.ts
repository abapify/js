export const enum Kind {
  Domain = 'Domain',
  Class = 'Class',
  Interface = 'Interface',
}

/**
 * SAP object types (as used in ADT endpoints)
 */
export const enum SapObjectType {
  DOMA = 'DOMA',
  CLAS = 'CLAS',
  INTF = 'INTF',
}
