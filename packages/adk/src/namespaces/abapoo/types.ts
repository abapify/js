/**
 * ABAP OO attributes (flattened on OO object roots)
 * Stored as strings in XML; domain layer can coerce to booleans when needed.
 */
export interface AbapOOAttrs {
  modeled?: string; // 'true' | 'false'
}
