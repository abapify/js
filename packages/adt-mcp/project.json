{
  "name": "adt-mcp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/adt-mcp/src",
  "projectType": "library",
  "tags": ["scope:adt", "type:mcp"],
  "targets": {
    "typecheck": {
      "command": "echo 'typecheck disabled: MCP SDK + Zod type inference causes OOM (see adt-mcp/AGENTS.md)'"
    }
  }
}
