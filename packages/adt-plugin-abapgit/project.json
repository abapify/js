{
  "name": "adt-plugin-abapgit",
  "targets": {
    "codegen": {
      "command": "bunx ts-xsd codegen --verbose",
      "options": {
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["ts-xsd:build"],
      "inputs": ["{projectRoot}/xsd/*.xsd", "{projectRoot}/ts-xsd.config.ts"],
      "outputs": ["{projectRoot}/src/schemas/generated"]
    },
    "test": {
      "command": "node --import tsx --test tests/**/*.test.ts",
      "options": {
        "cwd": "{projectRoot}"
      },
      "inputs": ["{projectRoot}/tests/**/*.ts", "{projectRoot}/src/**/*.ts"]
    }
  }
}
