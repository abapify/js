// Clean API (primary export)
export type { Package } from './types';
export { PackageAdapter } from './adapter';

// Technical API (for advanced users)
export { PackageSchema, type PackageXml } from './schema';
