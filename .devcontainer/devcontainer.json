{
  "name": "Node.js & TypeScript",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "customizations": {
    "vscode": {
      "extensions": ["nrwl.angular-console"]
    }
  },
  "runArgs": ["--network=host", "--cap-add=SYS_ADMIN"]
}
